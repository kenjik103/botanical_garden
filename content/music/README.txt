# Drop audio files (mp3/ogg/m4a/opus) here.
# The build scans this folder and emits output/music.json for the homepage player.
# Keep files compressed and under 25 MiB (Cloudflare Pages per-file cap).
